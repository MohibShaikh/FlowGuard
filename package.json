{
  "name": "n8n-flowguard",
  "version": "0.1.1",
  "description": "Security scanner for n8n workflow JSON files",
  "type": "module",
  "bin": {
    "flowguard": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "pretest": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "AGPL-3.0-only",
  "dependencies": {
    "chalk": "^5.6.2",
    "commander": "^14.0.3",
    "glob": "^13.0.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
